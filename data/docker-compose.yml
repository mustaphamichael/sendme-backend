version: '3'
services:
  database:
    container_name: sendme_db
    image: postgres:latest
    volumes:
      - dbdata:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    env_file:
      - ./secret.env

volumes:
  dbdata: